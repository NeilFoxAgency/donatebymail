import { useEffect, useState } from "react";

type Campaign = { slug: string; name: string; charityPledgeId: string; charityName: string; headline: string; summary: string; story: string; ctaLabel: string; heroImageUrl?: string };
export function CampaignPage({ slug }: { slug: string }) {
  const [campaign, setCampaign] = useState<Campaign | null>(null); const [message, setMessage] = useState("Loading campaign…");
  useEffect(() => { fetch(`/api/campaigns/${encodeURIComponent(slug)}`).then(async (r) => { const body = await r.json() as any; if (!r.ok) throw new Error(body.message || "Campaign unavailable."); setCampaign(body.campaign); setMessage(""); }).catch((e: Error) => setMessage(e.message)); }, [slug]);
  return <main className="campaign-public"><section className="campaign-hero">{message && <p role="status">{message}</p>}{campaign && <>{campaign.heroImageUrl && <img src={campaign.heroImageUrl} alt="" />}<p className="kicker">A Donate by Mail phone drive</p><h1>{campaign.headline}</h1><p className="campaign-summary">{campaign.summary}</p><p>Phones donated through this campaign support <strong>{campaign.charityName}</strong>.</p><a className="button primary" href={`/donate-phone?campaign=${encodeURIComponent(campaign.slug)}`}>{campaign.ctaLabel}</a></>}</section>{campaign && <section className="campaign-story"><h2>About this campaign</h2>{campaign.story.split(/\n{2,}/).map((paragraph, index) => <p key={index}>{paragraph}</p>)}<aside><h2>What happens next</h2><p>Tell us about your phone, prepare and mail it, and Donate by Mail will verify it after it arrives. We never ask for your passcode.</p></aside></section>}</main>;
}
