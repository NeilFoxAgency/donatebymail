import { FormEvent, useMemo, useState } from 'react';
import { ArrowRight, Check, CheckCircle2, ChevronDown, FileCheck2, HeartHandshake, Info, Leaf, LockKeyhole, Mail, Menu, PackageCheck, Plus, Printer, Recycle, ShieldCheck, Smartphone, Trash2, X } from 'lucide-react';

type Brand = 'Apple' | 'Samsung' | 'Google' | 'Motorola' | 'Other';
type Age = '0-1 year' | '2-3 years' | '4-5 years' | '6+ years';
type Condition = 'Excellent' | 'Good' | 'Fair' | 'Damaged';
type Storage = '64 GB or less' | '128 GB' | '256 GB' | '512 GB+';
type Device = { id: string; brand: Brand; age: Age; condition: Condition; storage: Storage; powersOn: boolean; unlocked: boolean };

const brands: Brand[] = ['Apple', 'Samsung', 'Google', 'Motorola', 'Other'];
const ages: Age[] = ['0-1 year', '2-3 years', '4-5 years', '6+ years'];
const conditions: Condition[] = ['Excellent', 'Good', 'Fair', 'Damaged'];
const storageOptions: Storage[] = ['64 GB or less', '128 GB', '256 GB', '512 GB+'];
const baseValues: Record<Brand, Record<Age, number>> = {
  Apple: { '0-1 year': 430, '2-3 years': 250, '4-5 years': 110, '6+ years': 35 },
  Samsung: { '0-1 year': 320, '2-3 years': 180, '4-5 years': 75, '6+ years': 25 },
  Google: { '0-1 year': 260, '2-3 years': 150, '4-5 years': 65, '6+ years': 22 },
  Motorola: { '0-1 year': 145, '2-3 years': 80, '4-5 years': 35, '6+ years': 12 },
  Other: { '0-1 year': 105, '2-3 years': 55, '4-5 years': 22, '6+ years': 8 },
};
const conditionFactor: Record<Condition, number> = { Excellent: 1, Good: 0.78, Fair: 0.48, Damaged: 0.18 };
const storageFactor: Record<Storage, number> = { '64 GB or less': 0.9, '128 GB': 1, '256 GB': 1.12, '512 GB+': 1.25 };
const makeDevice = (id: string): Device => ({ id, brand: 'Apple', age: '2-3 years', condition: 'Good', storage: '128 GB', powersOn: true, unlocked: true });
const estimate = (device: Device) => {
  const midpoint = baseValues[device.brand][device.age] * conditionFactor[device.condition] * storageFactor[device.storage] * (device.powersOn ? 1 : 0.38) * (device.unlocked ? 1.08 : 0.92);
  return { low: Math.max(1, Math.round(midpoint * 0.82)), high: Math.max(2, Math.round(midpoint * 1.17)) };
};
const money = (value: number) => new Intl.NumberFormat('en-US', { style: 'currency', currency: 'USD', maximumFractionDigits: 0 }).format(value);

function SelectField({ label, value, options, onChange }: { label: string; value: string; options: string[]; onChange: (value: string) => void }) {
  return <label className="block"><span className="mb-2 block text-sm font-semibold text-slate-700">{label}</span><span className="relative block"><select className="w-full appearance-none rounded-2xl border border-slate-200 bg-white px-4 py-3 pr-10 text-sm font-medium outline-none transition focus:border-emerald-600 focus:ring-4 focus:ring-emerald-100" value={value} onChange={(event) => onChange(event.target.value)}>{options.map((option) => <option key={option}>{option}</option>)}</select><ChevronDown className="pointer-events-none absolute right-3 top-3.5 h-4 w-4 text-slate-500" /></span></label>;
}

function App() {
  const [menuOpen, setMenuOpen] = useState(false);
  const [devices, setDevices] = useState<Device[]>([makeDevice('phone-1')]);
  const [submitted, setSubmitted] = useState(false);
  const [showReceipt, setShowReceipt] = useState(false);
  const [donorName, setDonorName] = useState('');
  const [donorEmail, setDonorEmail] = useState('');
  const [zipCode, setZipCode] = useState('');
  const [shippingMethod, setShippingMethod] = useState<'label' | 'kit'>('label');
  const totals = useMemo(() => devices.reduce((sum, device) => { const value = estimate(device); return { low: sum.low + value.low, high: sum.high + value.high }; }, { low: 0, high: 0 }), [devices]);
  const update = <K extends keyof Device>(id: string, key: K, value: Device[K]) => setDevices((current) => current.map((device) => device.id === id ? { ...device, [key]: value } : device));
  const addDevice = () => setDevices((current) => [...current, makeDevice(`phone-${Date.now()}`)]);
  const submit = (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); setSubmitted(true); setTimeout(() => document.getElementById('plan-complete')?.scrollIntoView({ behavior: 'smooth', block: 'center' }), 20); };
  const descriptions = devices.map((device, index) => `${index + 1}. ${device.brand} smartphone, approximately ${device.age} old, ${device.condition.toLowerCase()} condition, ${device.storage}, ${device.powersOn ? 'powers on' : 'does not power on'}`);

  return <div className="min-h-screen bg-stone-50 font-sans text-slate-900">
    <header className="sticky top-0 z-50 border-b border-emerald-950/10 bg-stone-50/90 backdrop-blur-xl"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-4 lg:px-8"><a href="#top" className="flex items-center gap-3"><span className="grid h-11 w-11 place-items-center rounded-2xl bg-emerald-900 text-white shadow-lg shadow-emerald-900/20"><Mail className="h-5 w-5" /></span><span><span className="block font-display text-xl font-bold leading-none text-emerald-950">Donate by Mail</span><span className="mt-1 block text-[11px] font-bold uppercase tracking-[0.2em] text-emerald-700">Old phones. New possibilities.</span></span></a><nav className="hidden items-center gap-8 text-sm font-semibold text-slate-700 md:flex"><a href="#how">How it works</a><a href="#estimate">Estimate value</a><a href="#security">Data security</a><a className="rounded-full bg-emerald-900 px-5 py-2.5 text-white" href="#donate">Donate a phone</a></nav><button className="grid h-11 w-11 place-items-center rounded-xl border bg-white md:hidden" onClick={() => setMenuOpen((value) => !value)} aria-label={menuOpen ? 'Close menu' : 'Open menu'}>{menuOpen ? <X className="h-5 w-5" /> : <Menu className="h-5 w-5" />}</button></div>{menuOpen && <nav className="grid gap-1 border-t bg-white px-5 py-4 md:hidden">{[['How it works', '#how'], ['Estimate value', '#estimate'], ['Data security', '#security'], ['Donate a phone', '#donate']].map(([label, href]) => <a className="rounded-xl px-3 py-3 font-semibold hover:bg-emerald-50" href={href} key={href} onClick={() => setMenuOpen(false)}>{label}</a>)}</nav>}</header>

    <main id="top">
      <section className="relative overflow-hidden bg-gradient-to-br from-emerald-950 via-emerald-900 to-teal-800 text-white"><div className="hero-grid absolute inset-0 opacity-20" /><div className="absolute -right-28 -top-32 h-96 w-96 rounded-full bg-amber-300/20 blur-3xl" /><div className="relative mx-auto grid max-w-7xl items-center gap-14 px-5 py-20 md:py-28 lg:grid-cols-[1.05fr_0.95fr] lg:px-8 lg:py-32"><div><div className="mb-7 inline-flex items-center gap-2 rounded-full border border-white/20 bg-white/10 px-4 py-2 text-xs font-bold uppercase tracking-[0.18em]"><HeartHandshake className="h-4 w-4" />A simpler way to give</div><h1 className="max-w-3xl font-display text-5xl font-bold leading-[1.02] tracking-tight sm:text-6xl lg:text-7xl">Your old phone still has a <span className="text-amber-300">future.</span></h1><p className="mt-6 max-w-2xl text-lg leading-8 text-emerald-50/90 sm:text-xl">Estimate its potential value, mail it with prepaid shipping, and turn unused technology into funding for meaningful causes.</p><div className="mt-9 flex flex-col gap-3 sm:flex-row"><a href="#estimate" className="inline-flex items-center justify-center gap-2 rounded-full bg-amber-300 px-6 py-3.5 font-bold text-emerald-950 shadow-xl transition hover:-translate-y-0.5">Estimate my phone <ArrowRight className="h-4 w-4" /></a><a href="#how" className="inline-flex items-center justify-center rounded-full border border-white/30 bg-white/10 px-6 py-3.5 font-bold">See how it works</a></div><div className="mt-10 flex flex-wrap gap-x-6 gap-y-3 text-sm text-emerald-50/90">{['Prepaid shipping', 'Secure data wiping', 'Printable acknowledgment'].map((item) => <span className="flex items-center gap-2" key={item}><Check className="h-4 w-4 text-amber-300" />{item}</span>)}</div></div><div className="relative mx-auto w-full max-w-md"><div className="absolute -left-8 -top-8 h-28 w-28 rounded-full bg-amber-300/30 blur-2xl" /><div className="relative rotate-2 rounded-[2.5rem] bg-white p-4 shadow-2xl"><div className="rounded-[2rem] bg-stone-100 p-7 text-slate-900"><div className="flex items-center justify-between"><span className="grid h-14 w-14 place-items-center rounded-2xl bg-emerald-100 text-emerald-900"><Smartphone className="h-7 w-7" /></span><span className="rounded-full bg-emerald-900 px-3 py-1 text-xs font-bold text-white">READY TO GIVE</span></div><p className="mt-12 text-sm font-bold uppercase tracking-[0.16em] text-emerald-700">One donated phone can become</p><div className="mt-4 grid grid-cols-2 gap-3"><div className="rounded-2xl bg-white p-4"><Leaf className="h-5 w-5 text-emerald-700" /><p className="mt-6 font-display text-3xl font-bold">Less</p><p className="text-sm text-slate-500">e-waste</p></div><div className="rounded-2xl bg-amber-200 p-4"><HeartHandshake className="h-5 w-5 text-emerald-900" /><p className="mt-6 font-display text-3xl font-bold">More</p><p className="text-sm text-emerald-900/70">impact</p></div></div></div></div></div></div></section>

      <section id="how" className="px-5 py-20 lg:px-8"><div className="mx-auto max-w-7xl"><div className="max-w-2xl"><p className="text-sm font-bold uppercase tracking-[0.18em] text-emerald-700">Three simple steps</p><h2 className="mt-3 font-display text-4xl font-bold text-emerald-950 sm:text-5xl">From forgotten drawer to real-world good.</h2></div><div className="mt-12 grid gap-5 md:grid-cols-3">{[[Smartphone, '1', 'Tell us about it', 'Answer a few quick questions to see an estimated resale-value range.'], [PackageCheck, '2', 'Mail it for free', 'Print a prepaid label or request a mailing kit, then send your phones securely.'], [FileCheck2, '3', 'Get your acknowledgment', 'After the phones arrive, receive a printable noncash donation acknowledgment.']].map(([Icon, number, title, copy]) => { const StepIcon = Icon as typeof Smartphone; return <article className="rounded-[2rem] border border-emerald-950/10 bg-white p-7 shadow-soft" key={String(number)}><div className="flex items-center justify-between"><span className="grid h-12 w-12 place-items-center rounded-2xl bg-emerald-100 text-emerald-900"><StepIcon className="h-6 w-6" /></span><span className="font-display text-5xl font-bold text-emerald-950/10">{number}</span></div><h3 className="mt-8 font-display text-2xl font-bold text-emerald-950">{title}</h3><p className="mt-3 leading-7 text-slate-600">{copy}</p></article>; })}</div></div></section>

      <section id="estimate" className="bg-emerald-950 px-5 py-20 text-white lg:px-8"><div className="mx-auto grid max-w-7xl gap-12 lg:grid-cols-[0.75fr_1.25fr]"><div><p className="text-sm font-bold uppercase tracking-[0.18em] text-amber-300">Instant estimate</p><h2 className="mt-3 font-display text-4xl font-bold sm:text-5xl">What could your phones be worth?</h2><p className="mt-5 leading-8 text-emerald-100/80">This rough estimate helps you understand potential resale proceeds. Final value depends on exact model, inspection, market demand, and resale costs.</p><div className="mt-8 rounded-2xl border border-amber-200/30 bg-amber-200/10 p-5 text-sm leading-6 text-amber-50"><Info className="mb-3 h-5 w-5 text-amber-300" /><strong>Not a tax appraisal.</strong> Donors determine the fair market value of noncash contributions. The official acknowledgment will describe the property but will not assign a value.</div></div><div><div className="space-y-5">{devices.map((device, index) => { const value = estimate(device); return <article className="rounded-[2rem] bg-stone-50 p-5 text-slate-900 shadow-2xl sm:p-7" key={device.id}><div className="mb-6 flex items-center justify-between"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-emerald-100 font-display text-lg font-bold text-emerald-900">{index + 1}</span><h3 className="font-display text-xl font-bold text-emerald-950">Phone {index + 1}</h3></div>{devices.length > 1 && <button className="grid h-10 w-10 place-items-center rounded-xl text-slate-400 hover:bg-red-50 hover:text-red-600" onClick={() => setDevices((current) => current.filter((item) => item.id !== device.id))} aria-label={`Remove phone ${index + 1}`}><Trash2 className="h-4 w-4" /></button>}</div><div className="grid gap-5 sm:grid-cols-2"><SelectField label="Brand" value={device.brand} options={brands} onChange={(value) => update(device.id, 'brand', value as Brand)} /><SelectField label="Approximate age" value={device.age} options={ages} onChange={(value) => update(device.id, 'age', value as Age)} /><SelectField label="Condition" value={device.condition} options={conditions} onChange={(value) => update(device.id, 'condition', value as Condition)} /><SelectField label="Storage" value={device.storage} options={storageOptions} onChange={(value) => update(device.id, 'storage', value as Storage)} /></div><div className="mt-5 grid gap-3 sm:grid-cols-2"><label className="flex items-center justify-between rounded-2xl border bg-white px-4 py-3"><span className="text-sm font-semibold">Does it power on?</span><input className="h-5 w-5 accent-emerald-700" type="checkbox" checked={device.powersOn} onChange={(event) => update(device.id, 'powersOn', event.target.checked)} /></label><label className="flex items-center justify-between rounded-2xl border bg-white px-4 py-3"><span className="text-sm font-semibold">Carrier unlocked?</span><input className="h-5 w-5 accent-emerald-700" type="checkbox" checked={device.unlocked} onChange={(event) => update(device.id, 'unlocked', event.target.checked)} /></label></div><div className="mt-6 flex flex-col gap-2 rounded-2xl bg-emerald-100 px-5 py-4 sm:flex-row sm:items-center sm:justify-between"><span className="text-sm font-bold text-emerald-900">Estimated device value</span><span className="font-display text-2xl font-bold text-emerald-950">{money(value.low)}-{money(value.high)}</span></div></article>; })}</div><button onClick={addDevice} className="mt-5 inline-flex w-full items-center justify-center gap-2 rounded-2xl border border-dashed border-emerald-300/60 bg-white/5 px-5 py-4 font-bold hover:bg-white/10"><Plus className="h-4 w-4" />Add another phone</button><div className="mt-7 rounded-[2rem] bg-amber-300 p-7 text-emerald-950 shadow-2xl"><p className="text-sm font-bold uppercase tracking-[0.16em]">Estimated combined value</p><p className="mt-2 font-display text-5xl font-bold">{money(totals.low)}-{money(totals.high)}</p><p className="mt-3 text-sm text-emerald-900/70">Illustrative range before inspection and resale expenses.</p><a href="#donate" className="mt-6 inline-flex items-center gap-2 rounded-full bg-emerald-950 px-6 py-3 font-bold text-white">Continue to donation <ArrowRight className="h-4 w-4" /></a></div></div></div></section>

      <section id="security" className="px-5 py-20 lg:px-8"><div className="mx-auto grid max-w-7xl items-center gap-12 lg:grid-cols-2"><div className="rounded-[2.5rem] bg-emerald-100 p-8 sm:p-12"><span className="grid h-16 w-16 place-items-center rounded-2xl bg-emerald-900 text-white"><LockKeyhole className="h-8 w-8" /></span><h2 className="mt-8 font-display text-4xl font-bold text-emerald-950">Your data deserves careful handling.</h2><p className="mt-5 leading-8 text-slate-700">Before mailing, back up what matters, sign out of Apple or Google accounts, turn off activation locks, remove SIM cards, and factory reset the phone when possible.</p></div><div className="space-y-4">{[['Back up your photos and files', 'Save anything you want to keep before resetting the device.'], ['Sign out and remove activation locks', 'Remove the device from Find My iPhone or your Google account.'], ['Factory reset and remove SIM cards', 'Never send us passwords, PINs, passcodes, or account credentials.'], ['Secure processing after arrival', 'Production operations will include documented chain-of-custody and verified wiping procedures.']].map(([title, copy], index) => <div className="flex gap-4 rounded-2xl border bg-white p-5" key={title}><span className="grid h-9 w-9 shrink-0 place-items-center rounded-full bg-emerald-100 font-bold text-emerald-900">{index + 1}</span><div><h3 className="font-bold text-emerald-950">{title}</h3><p className="mt-1 text-sm leading-6 text-slate-600">{copy}</p></div></div>)}</div></div></section>

      <section id="donate" className="bg-stone-200/60 px-5 py-20 lg:px-8"><div className="mx-auto grid max-w-6xl gap-10 lg:grid-cols-[0.8fr_1.2fr]"><div><p className="text-sm font-bold uppercase tracking-[0.18em] text-emerald-700">Ready when you are</p><h2 className="mt-3 font-display text-4xl font-bold text-emerald-950 sm:text-5xl">Request your prepaid mailing plan.</h2><p className="mt-5 leading-8 text-slate-600">This prototype demonstrates the full donor journey without transmitting personal information or creating a real carrier label.</p><div className="mt-8 space-y-3">{['Free prepaid shipping', 'Choose a printable label or mailed kit', 'Receipt issued only after verified receipt'].map((item) => <p className="flex items-center gap-3 font-semibold text-slate-700" key={item}><ShieldCheck className="h-5 w-5 text-emerald-700" />{item}</p>)}</div></div><div className="rounded-[2rem] bg-white p-6 shadow-soft sm:p-9">{!submitted ? <form onSubmit={submit}><div className="grid gap-5 sm:grid-cols-2"><label className="block"><span className="mb-2 block text-sm font-semibold">Full name</span><input required className="w-full rounded-2xl border px-4 py-3 outline-none focus:border-emerald-600 focus:ring-4 focus:ring-emerald-100" value={donorName} onChange={(event) => setDonorName(event.target.value)} /></label><label className="block"><span className="mb-2 block text-sm font-semibold">Email</span><input required type="email" className="w-full rounded-2xl border px-4 py-3 outline-none focus:border-emerald-600 focus:ring-4 focus:ring-emerald-100" value={donorEmail} onChange={(event) => setDonorEmail(event.target.value)} /></label><label className="block sm:col-span-2"><span className="mb-2 block text-sm font-semibold">ZIP code</span><input required inputMode="numeric" pattern="[0-9]{5}" className="w-full rounded-2xl border px-4 py-3 outline-none focus:border-emerald-600 focus:ring-4 focus:ring-emerald-100" value={zipCode} onChange={(event) => setZipCode(event.target.value)} placeholder="12345" /></label></div><fieldset className="mt-7"><legend className="text-sm font-semibold">How would you like to mail?</legend><div className="mt-3 grid gap-3 sm:grid-cols-2"><label className={`cursor-pointer rounded-2xl border p-4 ${shippingMethod === 'label' ? 'border-emerald-700 bg-emerald-50 ring-2 ring-emerald-100' : ''}`}><input className="sr-only" type="radio" checked={shippingMethod === 'label'} onChange={() => setShippingMethod('label')} /><Printer className="h-5 w-5 text-emerald-700" /><span className="mt-3 block font-bold">Printable label</span><span className="mt-1 block text-sm text-slate-500">Fastest. Use your own sturdy box.</span></label><label className={`cursor-pointer rounded-2xl border p-4 ${shippingMethod === 'kit' ? 'border-emerald-700 bg-emerald-50 ring-2 ring-emerald-100' : ''}`}><input className="sr-only" type="radio" checked={shippingMethod === 'kit'} onChange={() => setShippingMethod('kit')} /><Mail className="h-5 w-5 text-emerald-700" /><span className="mt-3 block font-bold">Mail-in kit</span><span className="mt-1 block text-sm text-slate-500">We send packaging and instructions.</span></label></div></fieldset><label className="mt-7 flex items-start gap-3 rounded-2xl bg-stone-100 p-4"><input required className="mt-1 h-5 w-5 shrink-0 accent-emerald-700" type="checkbox" /><span className="text-sm leading-6 text-slate-700">I understand that I should back up my data, sign out of accounts, remove activation locks, and factory reset each phone when possible. I will not include passwords or passcodes.</span></label><button type="submit" className="mt-6 inline-flex w-full items-center justify-center gap-2 rounded-full bg-emerald-900 px-6 py-4 font-bold text-white">Create my mailing plan <ArrowRight className="h-4 w-4" /></button><p className="mt-3 text-center text-xs text-slate-500">Prototype only. This form stores nothing and does not create a real shipping label.</p></form> : <div id="plan-complete" className="py-4 text-center"><span className="mx-auto grid h-16 w-16 place-items-center rounded-full bg-emerald-100 text-emerald-800"><CheckCircle2 className="h-8 w-8" /></span><p className="mt-6 text-xs font-bold uppercase tracking-[0.18em] text-emerald-700">Preview complete</p><h3 className="mt-2 font-display text-3xl font-bold text-emerald-950">Your mailing plan is ready.</h3><p className="mx-auto mt-4 max-w-lg leading-7 text-slate-600">In production, {shippingMethod === 'label' ? 'a prepaid label' : 'a mail-in kit confirmation'} would be sent to <strong>{donorEmail}</strong>. The acknowledgment becomes available only after Donate by Mail receives the devices.</p><div className="mt-7 flex flex-col justify-center gap-3 sm:flex-row"><button onClick={() => { setShowReceipt(true); setTimeout(() => document.getElementById('receipt')?.scrollIntoView({ behavior: 'smooth' }), 20); }} className="inline-flex items-center justify-center gap-2 rounded-full bg-emerald-900 px-6 py-3 font-bold text-white"><Printer className="h-4 w-4" />Preview printable receipt</button><button onClick={() => setSubmitted(false)} className="rounded-full border px-6 py-3 font-bold">Edit details</button></div></div>}</div></div></section>

      {showReceipt && <section className="receipt-wrap bg-slate-900/5 px-5 py-20 lg:px-8"><div id="receipt" className="receipt mx-auto max-w-4xl bg-white p-7 shadow-soft sm:p-12"><div className="sample-watermark">SAMPLE</div><div className="relative"><div className="no-print mb-8 flex flex-col justify-between gap-4 border-b pb-6 sm:flex-row sm:items-center"><div><p className="font-bold text-emerald-950">Printable acknowledgment preview</p><p className="mt-1 text-sm text-slate-500">Print it or save it as a PDF.</p></div><div className="flex gap-2"><button onClick={() => window.print()} className="inline-flex items-center gap-2 rounded-full bg-emerald-900 px-5 py-2.5 font-bold text-white"><Printer className="h-4 w-4" />Print</button><button onClick={() => setShowReceipt(false)} className="rounded-full border px-5 py-2.5 font-bold">Close</button></div></div><div className="flex flex-col justify-between gap-8 border-b-2 border-emerald-950 pb-8 sm:flex-row"><div className="flex items-center gap-4"><span className="grid h-14 w-14 place-items-center rounded-2xl bg-emerald-900 text-white"><Mail className="h-6 w-6" /></span><div><h2 className="font-display text-3xl font-bold text-emerald-950">Donate by Mail</h2><p className="mt-1 text-sm font-semibold text-slate-600">501(c)(3) nonprofit - EIN 92-1515120</p></div></div><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-slate-500">Acknowledgment ID</p><p className="mt-1 font-mono font-bold">DBM-PREVIEW-2026</p></div></div><div className="py-9"><p className="inline-flex rounded-full bg-amber-100 px-4 py-2 text-xs font-bold uppercase tracking-[0.14em] text-amber-900">Sample only - not valid for tax purposes</p><h3 className="mt-6 font-display text-3xl font-bold">Noncash charitable contribution acknowledgment</h3><p className="mt-6 leading-7 text-slate-700">Thank you, <strong>{donorName || 'Donor'}</strong>. This sample shows the acknowledgment format issued after the organization physically receives and verifies the donated property.</p><dl className="mt-8 grid gap-5 rounded-2xl border p-6 sm:grid-cols-2"><div><dt className="text-xs font-bold uppercase tracking-[0.14em] text-slate-500">Contribution date</dt><dd className="mt-1 font-semibold">Issued after verified receipt</dd></div><div><dt className="text-xs font-bold uppercase tracking-[0.14em] text-slate-500">Donor email</dt><dd className="mt-1 font-semibold">{donorEmail || 'Not provided'}</dd></div><div className="sm:col-span-2"><dt className="text-xs font-bold uppercase tracking-[0.14em] text-slate-500">Property received</dt><dd className="mt-3 space-y-2 text-sm leading-6 text-slate-700">{descriptions.map((description) => <p key={description}>{description}</p>)}</dd></div></dl><div className="mt-8 rounded-2xl bg-emerald-50 p-6 text-sm leading-7 text-emerald-950"><strong>No goods or services were provided by Donate by Mail in exchange for this contribution.</strong></div><p className="mt-8 text-sm leading-7 text-slate-600">Donate by Mail has not provided or confirmed a fair market value for this property. The donor is responsible for determining and substantiating any charitable deduction.</p></div><div className="border-t pt-6 text-xs leading-5 text-slate-500">Website-design preview only. A production receipt should be generated from a verified intake record after the phones are received.</div></div></div></section>}

      <section className="bg-amber-300 px-5 py-16 lg:px-8"><div className="mx-auto flex max-w-6xl flex-col items-center justify-between gap-7 text-center md:flex-row md:text-left"><div><p className="text-sm font-bold uppercase tracking-[0.18em] text-emerald-800">One drawer. One box. One good decision.</p><h2 className="mt-2 font-display text-3xl font-bold text-emerald-950 sm:text-4xl">Give that old phone somewhere better to be.</h2></div><a href="#estimate" className="inline-flex shrink-0 items-center gap-2 rounded-full bg-emerald-950 px-7 py-4 font-bold text-white">Start an estimate <Recycle className="h-4 w-4" /></a></div></section>
    </main>

    <footer className="bg-emerald-950 px-5 py-12 text-emerald-50 lg:px-8"><div className="mx-auto grid max-w-7xl gap-8 md:grid-cols-[1fr_auto] md:items-end"><div><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-white/10"><Mail className="h-5 w-5" /></span><span className="font-display text-xl font-bold">Donate by Mail</span></div><p className="mt-4 max-w-xl text-sm leading-6 text-emerald-100/70">Turning unused phones into support for meaningful causes through a simple, secure mail-in experience.</p></div><div className="text-sm text-emerald-100/60 md:text-right"><p>© 2026 Donate by Mail</p><p className="mt-2">Prototype website - Privacy and terms to be added before launch</p></div></div></footer>
  </div>;
}

export default App;
